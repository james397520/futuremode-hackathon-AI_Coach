# Reference only: .env is not loaded automatically. Export variables in your shell.
# FastAPI: mock (default) or local (Ollama qwen3:8b at localhost:11434).
COACH_AI=mock
# Relative to the working directory; omit to use backend/data automatically.
COACH_DATA_DIR=data

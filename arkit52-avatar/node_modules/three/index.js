// 轉接墊:tools/ 的 Node 腳本與 public/vendor/jsm/* 內部都會 import 'three'。
// 瀏覽器靠 index.html 的 importmap 解析裸模組名,Node 沒有,而 Node 是從 import 者
// 自己的位置往上找 node_modules,所以這個墊片必須放在專案根目錄。
// 只有兩個檔案不到 1KB —— 不是把 three 複製一份。
export * from '../../public/vendor/three.module.js';
